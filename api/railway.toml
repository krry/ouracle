[build]
builder = "railpack"
buildCommand = "bun install && bun run sync-decks"
watchPatterns = ["api/**"]

[deploy]
startCommand = "bun start"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
