[build]
builder = "railpack"
buildCommand = "bun install && bun run sync-decks && bun run download-ambient"
watchPatterns = ["api/**"]

[deploy]
startCommand = "bun start"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
