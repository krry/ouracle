[build]
builder = "railpack"
buildCommand = "bun install"
watchPatterns = ["api/**"]

[deploy]
startCommand = "bun run sync-decks && bun start"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
